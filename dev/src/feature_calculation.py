class Calculation:
    def __init__(self, operator):
        self.operator = operator

    # +연산자 메소드
    def Add(self):
                
            if self.operator == '+':
                result = int(result) + int(num)
                return result 
    # -연산자 메소드
    def Sub(self):
    
            if self.operator == '-':
                result = int(result) - int(num)
                return result
    # *연산자 메소드            
    def Mul(self):
                        
            if self.operator == '*':
                result = int(result) * int(num)
                return result
        